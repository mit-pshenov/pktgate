[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Verbose output with short summary
addopts = -v --tb=short -x
# Markers
markers =
    slow: tests that take a long time (e.g., rate-limit burst)
