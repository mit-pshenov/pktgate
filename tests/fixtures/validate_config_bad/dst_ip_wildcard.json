{
  "_comment": "P0-01: dst_ip would silently become a 0.0.0.0/0 LPM wildcard. validate_config MUST reject this.",
  "device_info": { "interface": "eth0" },
  "pipeline": {
    "layer_3": [
      { "rule_id": 1, "match": { "dst_ip": "10.0.0.0/24" }, "action": "drop" }
    ]
  },
  "default_behavior": "drop"
}
